app = "avalonstar-tv"
primary_region = "sjc"

[build]

[env]
  # Tailscale auth key will be set as a secret

[http_service]
  internal_port = 8080
  force_https = true
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
